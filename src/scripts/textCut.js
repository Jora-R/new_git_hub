// var unnecessaryWords = /^(Как|Я хотел бы|Как я могу)/;
        
        
// bind(
//     "preMatch",
//     function($context) {
//     if (unnecessaryWords) {
//         $context.request.query.replace(unnecessaryWords, '');
//         }
// });  
    
// log("Запрос был такой:" + $context.request.query);